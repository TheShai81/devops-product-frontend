# Ecommerce Frontend

This is the frontend UI to interact with the ECommerce platforms. You can view
the products we offer and place orders on the website.

This microservice runs on Python Version >3.10.
To install all dependencies, run `pip install -r requirements.txt`.
You may choose to create a venv for this.